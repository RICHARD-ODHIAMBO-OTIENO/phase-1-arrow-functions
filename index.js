const divide = function() {
    return 2000/100;
}

// An arrow fuction called square
const square = (side) => {
    return side ** 2;
}

// Create an arrow function called add
const add = (length1, length2) => {
    return length1 + length2
}